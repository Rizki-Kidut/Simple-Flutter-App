import 'package:flutter/material.dart';

const primary = Color.fromRGBO(44, 27, 228, 1);
const secondary = Color.fromRGBO(247, 250, 252, 1);
const header = Color.fromRGBO(92, 107, 143, 1);
const error = Color.fromRGBO(227, 23, 63, 1);

const defaultPadding = 16;